#ifndef BINARY_H
#define BINARY_H


typedef struct node{
	
	
}node;

bool sort(Hypercubes **array, int N) {
	for (i = 0; i < N; i++) {
		int low = 0;
		int high = 0;
		int node;
		while (low <= high ) {
			mid = (low + high) / 2;
			if (Hypercubes **array == Key) 
			{
				low = mid + 1;
			}
			else if (Hypercubes **array == Key) 
			{
				high = mid - 1; 
			}
		}
	}
	
}

/* This not finished but it's start. It won't run becasue I don't have a "key" 

So the "key" is the coord and now I just have to replace but instead of "==" we need to use "<", ">" 

I am curretnly working with the Python Hysord */








#endif
